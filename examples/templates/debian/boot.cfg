# Shell-engine template: only %%{NAME} substitutes; a bare % is literal text,
# so this comment survives and so does a shell line like: date +%Y%m%d
default=install
label install
    kernel %{IMAGE_GLOBALS_KERNEL}
    append ip=%{TARGET_IP} hostname=%{TARGET_HOSTNAME} domain=%{DHCPZONE_DOMAIN}
